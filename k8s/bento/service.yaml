apiVersion: v1
kind: Service
metadata:
  name: bento-price-predictor
  namespace: app
  labels:
    app: bento-price-predictor
spec:
  type: ClusterIP
  selector:
    app: bento-price-predictor
  ports:
    - name: http
      port: 80
      targetPort: http
